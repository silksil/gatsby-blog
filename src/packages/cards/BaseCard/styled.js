import styled from "styled-components";

const Wrapper = styled.div`
  border-radius: 0.9rem;
  background: #fff;
  box-shadow: 0 0.125rem 1rem 0 rgba(198, 203, 222, 0.45);
`;

export default Wrapper;
